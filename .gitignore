# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

!node_modules/.vite

**/tempobook/dynamic/
**/tempobook/storyboards/

core

.github/

**/tempobook/**
.vercel

# Material de referencia (PDFs fuente, folletos de ejemplo, capturas, HTML de mapas).
# Se mantiene en disco pero fuera del repo para no inflarlo.
reference/

# Datos personales de leads y contactos exportados de GHL.
# Este repositorio es público: son nombres, teléfonos y correos de gente real.
# Publicarlos no se puede deshacer — borrarlos después no los saca del historial.
leads.csv
leads-enriquecido.csv
leads-enriquecido.json
backup-*.json
docs/contactos-ghl/

# Material pesado de trabajo: fotos originales, piezas de marketing, planos fuente.
# Cerca de 1 GB que no tiene por qué viajar en cada clon, con archivos sueltos
# de más de 50 MB que GitHub rechazaría.
Fotos proyectos/
marketing/
PLANOS CHECK/

# Generado por scripts/build-content.mjs en cada build
api/_content/
